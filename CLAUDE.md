# CLAUDE.md

このリポジトリの Claude Code 向け設定。

## 位置づけ

Geckou が自社の制作案件で使う UI コンポーネント集（`packages/` 配下を npm へ公開する）。
ロジックをフレームワーク非依存の core に集約し、Vue と React が同じ core を使う。
パッケージの一覧と設計方針は `README.md` を参照。

**プロダクトではなくライブラリ**なので、進め方の規約は project-starter とは別に持つ。
仕様書・ロードマップ・機能ステータス表は無い。**正は公開 API とテスト**。

デフォルトブランチは `main`。

## 自律性の境界

判断が必要になっても、そのたびにセッションを止めない。原則は
「**避けられるなら避ける、避けられないなら聞く**」。推測で決めて進めることはしない。

**1. そのまま進める**

自分たちのライブラリなので、**公開 API を変えない範囲はどんどん直してよい**。

- バグ修正、内部実装のリファクタ、テストの追加
- 型の内部的な整理（公開シグネチャが変わらないもの）
- ドキュメント・README の整合更新
- 作業ブランチへの push と PR の作成（→「PR は出す、マージは人が決める」）

**2. その作業を保留して、別の作業へ移る（Issue に切る）**

判断が要るものは Issue に `question` ラベルを付けて切り、その作業は保留して別の作業へ移る。
利用側（派生プロジェクト）に影響が出るものが該当する。

- **公開 API の追加・シグネチャ変更**（props・emit・型。一度出すと消せない。追加も含む）
- 新しいコンポーネントの追加、既存コンポーネントの見た目・挙動の変更
- core に置くか各フレームワーク側に置くかの判断
- 依存パッケージの追加・メジャーアップデート、peerDependencies の範囲変更

**進められる作業が尽きたら、そこで聞く。** Issue は待ち行列であって、逃げ場ではない。

**3. その場で止めて聞く（Issue に切らない）**

取り消せない・外に出る操作。他に作業が残っていても、これ自体は確認が取れるまで実行しない。

- **`<パッケージ>@<バージョン>` 形式のタグを push すること**
  （`.github/workflows/publish.yml` が発火して `npm publish` が走る。npm は公開後の
  取り消しが実質できない）
- **PR のマージ**
- 履歴の書き換え（force push）、ブランチやファイルの削除
- **このリポジトリ以外**への Issue・PR の投稿

## PR は出す、マージは人が決める

作業が一区切りしたら、確認を待たずに push して PR を作る。人がやるのはマージの判断だけ。

- 粒度は **1 関心事 = 1 PR**。「ついでの修正」を混ぜない（混ざるとマージの判断が重くなる）
- PR 本文には**何を確認してほしいか**を書く。公開 API に触れる変更なら、
  利用側にどう影響するかを明記する
- `yarn type-check` / `yarn test` / `yarn build` が通り、セルフレビューを通してから出す。
  赤い PR も、自分で読めば分かる指摘が残った PR も人に見せない

## 見つけた問題は Issue に切る

作業中に別の問題を見つけたとき、**今の差分と同じ関心事なら、その場で直す**。
違う関心事なら **Issue に切って、今の PR には混ぜない**。判断は大きさではなく関心事で行う。

立てる前に、**実在を確認する**（推測で立てない）。**既存 Issue を検索して重複を避ける**。
立てた Issue は自分で対応する。PR 本文に `Closes #<番号>` を書けばマージで閉じる。

## 差分のセルフレビュー

PR を出す前に、**サブエージェントに差分だけを渡して読ませる**。書いた経緯・意図は渡さない。

書き手は意図を握った状態で読むためコードが意図どおりに見える。レビューが機能するのは
読み手が意図を知らないからで、同じ文脈で読み直しても結論は変わらない。

観点は通常のコードレビューに加えて、**差分の外との接続**を必ず見る。実際に問題が出るのは
ほぼここ（編集したファイル単体では正しく、外との関係で壊れている）。

- この差分で変えた値・名前・**公開 API** を参照している箇所が、差分の外にないか
  （core ↔ vue ↔ react の対応、demo、README、型定義、利用側の想定）
- core を変えたとき、**Vue と React の両方**に追従が要らないか
- リポジトリの外の状態に依存していないか（npm 上の公開済みバージョン、GitHub の設定）
- 同じ事実が二か所に書かれていて片方だけ直っていないか

指摘に「それはこういう理由で正しい」と説明したくなったら、**その理由が差分から読み取れて
いない**ということ。コメントか命名で差分の中に入れる。

## Copilot の自動レビュー

PR ごとに手でレビューを依頼しなくて済むよう、ruleset で常時 ON にする。
定義は `.github/rulesets/copilot-review.json`。1 回だけ取り込む:

```bash
gh api repos/geckou/ui/rulesets --method POST \
  --input .github/rulesets/copilot-review.json
```

## よく使うコマンド

```bash
yarn dev          # デモサイトの開発サーバー
yarn type-check   # 全パッケージの型チェック
yarn test         # テスト実行
yarn lint         # ESLint
yarn build        # 全パッケージのビルド
yarn release      # リリース（タグ push は「その場で止めて聞く」対象）
```
