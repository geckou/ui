{
  "extends": "./tsconfig.json",
  // vitest.config.ts は globals を有効にしていない（テストは describe / it を
  // 明示 import する）。types に vitest/globals を入れると、グローバルに
  // 生えていない API が型の上だけ通ってしまう
  "include": ["src", "tests"]
}
